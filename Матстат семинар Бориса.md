
# Теория вероятностей и математическая статистика

## 1. Организационные требования (Типовой расчет)
- **Срок сдачи:** к 9 апреля.
- **Состав работы:** Основной `.doc` файл (перевести в `.pdf`), варианты, 3 задания.
- **Формат названия:** `ИС_Группа_03-23_Фамилия_ТР...`
- **Проверка:** ТР проверяется **только** на занятиях. Нет ТР $\rightarrow$ нет зачета.
- **Код:** Писать на Python, разрешено использование библиотек. Сдавать только `.py` файл с кодом.
- **Структура отчета:**
  - Краткие сведения.
  - Основной раздел: Задание 1, Задание 2, Задание 3 (каждое с новой страницы).
  - Вариант, параметры ($n$, $p$ и т.д.).
  - Таблица (200 чисел): 20 строк, 10 столбцов, сортировка, результаты.
- **Требуемые вычисления:** Эмпирическая функция распределения, вариационный ряд, полигон частот, средние характеристики (мода, медиана). Считать по формулам с помощью эмпирической выборки.

---

## 2. Основные распределения случайных величин

| Распределение | $P_k$ (для дискр.) или Плотность (для непр.) | $M\xi$ (Мат. ожидание) | $D\xi$ (Дисперсия) |
| :--- | :--- | :--- | :--- |
| **Биномиальное** $Bi(n,p)$ | $C_n^k p^k q^{n-k}$ | $np$ | $npq$ |
| **Пуассона** $P(\lambda)$ | $\frac{\lambda^k}{k!} e^{-\lambda}$ | $\lambda$ | $\lambda$ |
| **Геометрическое** $G(p)$ | $p q^{k-1} \ (k \ge 1)$ | $\frac{1}{p}$ | $\frac{q}{p^2}$ |
| **Равномерное (дискр.)** $U(n)$ | $\frac{1}{n} \ (k=1..n)$ | $\frac{n+1}{2}$ | $\frac{n^2-1}{12}$ |
| **Гипергеометрическое** | $\frac{C_K^k C_{N-K}^{n-k}}{C_N^n}$ | $n\frac{K}{N}$ | $n\frac{K}{N}\frac{N-K}{N}\frac{N-n}{N-1}$ |
| **Нормальное** $N(m, \sigma^2)$ | $\frac{1}{\sigma\sqrt{2\pi}} e^{-\frac{(x-m)^2}{2\sigma^2}}$ | $m$ | $\sigma^2$ |
| **Показательное (Exp)** | $\lambda e^{-\lambda x}$ | $\frac{1}{\lambda}$ | $\frac{1}{\lambda^2}$ |
| **Равномерное (непр.)** $U([a,b])$ | $\frac{1}{b-a}$ | $\frac{a+b}{2}$ | $\frac{(b-a)^2}{12}$ |

---

## 3. Вычисление моментов и коэффициентов

### 3.1. Показательное распределение
Связь центрального момента 3-го порядка с начальными:
$$\mu_3^0 = M(\xi - M\xi)^3 = \mu_3 - 3\mu_2\mu_1 + 2\mu_1^3$$
Для показательного распределения начальные моменты $M\xi^k = \frac{k!}{\lambda^k}$:
- $\mu_1 = M\xi = \frac{1}{\lambda}$
- $\mu_2 = M\xi^2 = \frac{2}{\lambda^2}$
- $D\xi = \mu_2 - (\mu_1)^2 = \frac{2}{\lambda^2} - \frac{1}{\lambda^2} = \frac{1}{\lambda^2}$
- $\mu_3 = M\xi^3 = \frac{6}{\lambda^3}$

Центральный момент 3-го порядка:
$$\mu_3^0 = \frac{6}{\lambda^3} - 3\left(\frac{2}{\lambda^2}\right)\left(\frac{1}{\lambda}\right) + 2\left(\frac{1}{\lambda}\right)^3 = \frac{2}{\lambda^3}$$
Коэффициент асимметрии:
$$\gamma_1 = \frac{\mu_3^0}{\sigma^3} = \frac{2/\lambda^3}{(1/\lambda)^3} = 2$$

### 3.2. Равномерное распределение $U([a, b])$
- Нормальное распределение: коэфф. асимметрии $0$.
- Мат. ожидание: $\mu_1 = \frac{a+b}{2}$
- Дисперсия: $\sigma^2 = \mu_2^0 = \frac{(b-a)^2}{12}$

Центральный момент 4-го порядка:
$$\mu_4^0 = \frac{1}{b-a} \int_a^b (x-\mu_1)^4 dx$$
Замена $y = x - \mu_1$:
$$\mu_4^0 = \frac{1}{b-a} \int_{-\frac{b-a}{2}}^{\frac{b-a}{2}} y^4 dy = \frac{2}{b-a} \int_0^{\frac{b-a}{2}} y^4 dy = \frac{2}{b-a} \cdot \frac{1}{5} \cdot \frac{(b-a)^5}{2^5} = \frac{(b-a)^4}{80}$$

Коэффициент эксцесса:
$$\gamma_2 = \frac{\mu_4^0}{\sigma^4} - 3 = \frac{(b-a)^4 / 80}{((b-a)^2 / 12)^2} - 3 = \frac{144}{80} - 3 = \frac{9}{5} - 3 = -\frac{6}{5}$$


## 4. Точечные оценки

> **Постановка задачи:** Есть некоторая функция распределения $F(x, \theta)$. Вид распределения предполагается известным (например, равномерное), но параметр $\theta$ неизвестен. Его нужно оценить по имеющимся данным.

- **Выборка:** $\vec{x} = (x_1, ..., x_n)$, где $\{x_j\}$ — н.о.р.с.в. (независимые одинаково распределенные случайные величины).
- **Оценка ($\hat{\theta}(\vec{x})$):** это функция от выборки (статистика), используемая для поиска неизвестного значения параметра. Функция от случайного вектора сама является случайной величиной.



### Свойства оценок:
1. **Несмещенность:** Математическое ожидание оценки равно истинному значению параметра. Характеризует близость оценки к истинному значению "в среднем".
   $$M\hat{\theta}_N(\vec{x}) = \theta$$
2. **Состоятельность:** При увеличении объема выборки оценка сходится по вероятности к истинному значению параметра (по Закону Больших Чисел).
   $$\lim_{N\to\infty} P(|\hat{\theta}_N(\vec{x}) - \theta| < \varepsilon) = 1$$
   $$\hat{\theta}_N(\vec{x}) \xrightarrow[N\to\infty]{P} \theta$$
   Или эквивалентно: $P(|\hat{\theta}_N(\vec{x}) - \theta| \ge \varepsilon) \xrightarrow{N\to\infty} 0$.


## 5. Примеры

### Пример: Метод моментов для $U([0, \theta])$
Мат. ожидание: $\mu_1 = \frac{\theta}{2}$.
Выборочное среднее: $\bar{\mu}_1(\vec{x}) = \frac{1}{N} \sum_{j=1}^N x_j$ (по вероятности стремится к мат. ожиданию).
Приравниваем и выражаем оценку:
$$\hat{\theta}_N^{(1)}(\vec{x}) = \frac{2}{N} \sum_{j=1}^N x_j$$

Проверка несмещенности:
$$M(\hat{\theta}_N^{(1)}) = \frac{2}{N} \left( N \cdot \frac{\theta}{2} \right) = \theta$$
Оценка несмещенная $\checkmark$

Проверка состоятельности (через неравенство Чебышева):
Общая теорема ЗБЧ использует неравенство Чебышева: $P(|\xi - M\xi| \ge \varepsilon) \le \frac{D\xi}{\varepsilon^2}$.
Дисперсия оценки: 
$$D(\hat{\theta}_N^{(1)}) = \frac{4}{N^2} \left( N \cdot \frac{\theta^2}{12} \right) = \frac{\theta^2}{3N}$$
Тогда:
$$P\left(|\hat{\theta}_N^{(1)} - \theta| \ge \varepsilon\right) \le \frac{\theta^2}{3N \cdot \varepsilon^2} \xrightarrow{N\to\infty} 0$$
Вероятность отклонения стремится к нулю $\Rightarrow$ состоятельность доказана.

### Задачи для самостоятельной работы (Д/З)
1. **Проверить оценку** $\hat{\theta}_N^{(2)} = N \cdot \min(x_j)$.
   *Требуется доказать несмещенность и состоятельность.*

2. **Задача "на подумать":** Оценка через порядковую статистику.
   $$\hat{\theta}_N^{(3)} = \max(x_j) \equiv X_{(N)}$$
   *Требуется найти плотность, посчитать интеграл:* $M X_{(N)} = \int_0^\theta x \cdot f_N^{(N)}(x) dx$

равн. распределение $U([0, \theta])$. 
* Функция распределения: $F(x) = \frac{x}{\theta}$ при $x \in [0, \theta]$.
* Плотность вероятности: $f(x) = \frac{1}{\theta}$ при $x \in [0, \theta]$.

### 1. Проверка оценки $\hat{\theta}_N^{(2)} = N \cdot \min(x_j)$

 $X_{(1)} = \min(x_j)$ - первая порядковая статистика

Найдем функцию распределения $X_{(1)}$:
$$F_{X_{(1)}}(x) = P(\min(x_j) < x) = 1 - P(\min(x_j) \ge x)$$
Так как элементы выборки независимы:
$$P(\min(x_j) \ge x) = P(x_1 \ge x \cap \dots \cap x_N \ge x) = (1 - F(x))^N = \left(1 - \frac{x}{\theta}\right)^N$$
$$F_{X_{(1)}}(x) = 1 - \left(1 - \frac{x}{\theta}\right)^N$$
Найдем плотность распределения $X_{(1)}$:
$$f_{X_{(1)}}(x) = \frac{d}{dx} F_{X_{(1)}}(x) = \frac{N}{\theta} \left(1 - \frac{x}{\theta}\right)^{N-1}$$

мат. ож. $X_{(1)}$:
$$M X_{(1)} = \int_0^\theta x \cdot \frac{N}{\theta} \left(1 - \frac{x}{\theta}\right)^{N-1} dx$$
Сделаем замену $y = 1 - \frac{x}{\theta}$,=> $x = \theta(1-y)$ и $dx = -\theta dy$:
$$M X_{(1)} = \int_1^0 \theta(1-y) \cdot \frac{N}{\theta} y^{N-1} (-\theta) dy = N\theta \int_0^1 (y^{N-1} - y^N) dy$$
$$M X_{(1)} = N\theta \left[ \frac{y^N}{N} - \frac{y^{N+1}}{N+1} \right]_0^1 = N\theta \left( \frac{1}{N} - \frac{1}{N+1} \right) = \frac{\theta}{N+1}$$
$$M\hat{\theta}_N^{(2)} = M(N \cdot X_{(1)}) = N \cdot \frac{\theta}{N+1} = \frac{N}{N+1}\theta \neq \theta$$
 Оценка смещенная

Состоятельность:
Оценка состоятельна, если $\hat{\theta}_N^{(2)} \xrightarrow{P} \theta$. 
$$P(\hat{\theta}_N^{(2)} < x) = P(N \cdot X_{(1)} < x) = P\left(X_{(1)} < \frac{x}{N}\right) = 1 - \left(1 - \frac{x}{N\theta}\right)^N$$
$$\lim_{N\to\infty} \left[ 1 - \left(1 - \frac{x/\theta}{N}\right)^N \right] = 1 - e^{-\frac{x}{\theta}}$$
Оценка несостоятельная. 



 
 
 $\hat{\theta}_N^{(3)} = \max(x_j) \equiv X_{(N)}$

$X_{(N)} = \max(x_j)$.

Плотность $X_{(N)}$
Сначала найдем функцию распределения $X_{(N)}$:
$$F_{X_{(N)}}(x) = P(\max(x_j) < x)$$
Максимум меньше $x$ только в том случае, если *все* элементы выборки меньше $x$:
$$P(x_1 < x \cap x_2 < x \cap \dots \cap x_N < x) = \prod_{j=1}^N P(x_j < x) = (F(x))^N$$
Подставляем функцию равномерного распределения:
$$F_{X_{(N)}}(x) = \left(\frac{x}{\theta}\right)^N$$
Теперь находим плотность, беря производную по $x$:
$$f_{X_{(N)}}(x) = \frac{d}{dx} \left( \frac{x^N}{\theta^N} \right) = \frac{N \cdot x^{N-1}}{\theta^N}$$


$$M X_{(N)} = \int_0^\theta x \cdot f_{X_{(N)}}(x) dx = \int_0^\theta x \cdot \frac{N \cdot x^{N-1}}{\theta^N} dx$$
$$M X_{(N)} = \frac{N}{\theta^N} \int_0^\theta x^N dx$$

$$M X_{(N)} = \frac{N}{\theta^N} \cdot \left. \left( \frac{x^{N+1}}{N+1} \right) \right|_0^\theta = \frac{N}{\theta^N} \cdot \frac{\theta^{N+1}}{N+1} = \frac{N}{N+1}\theta$$
смещенная
