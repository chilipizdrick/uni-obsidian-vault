---
tags:
  - lecture
  - 2024-02-16
  - 2nd-semester
  - ангем
---

> Creation time: 14:21

$f(x)=f(x_{1},x_{2},\dots,x_{n})$

### Определение

> Коэффициентами при приращении аргумента являются именно частные производные

> T. f дифф. в точке $X_{0}=(x_{10},\dots,x_{n_{0}})\implies \exists \frac{\partial f}{\partial x_{1}}(x_{0}),\dots, \frac{\partial f}{\partial x_{n}}(x_{0})$
> (необх. условие дифф.); если $\exists \frac{\partial f}{\partial x_{1}}(X)$,  $\forall x  \in U(X_{0}) и \frac{\partial f}{\partial x_{1}},\dots, \frac{\partial f}{\partial x_{n}}$ непрерывны, то f дифф. в точке $X_{0}$(достаточное условие дифф.), приращение в точке $X_{0}$ ($\Delta f(x_{0})=f(x_{0}+\Delta x)-f(x_{0})= \frac{\partial f}{\partial x_{1}}(x_{0})\Delta x_{1}+\dots+ \frac{\partial f}{\partial x_{n}}(x_{0})\Delta x_{n} + \bar{o}(\text{mod }\Delta x)$), где $\Delta x=(\Delta x_{1},\dots,\Delta x_{n}), \text{mod }\Delta X=\sqrt{ (\Delta x_{1})^{2}+\dots+(\Delta x_{n})^{2} }$ при $| \Delta x|\to 0$

> Доказательство. Достаточное условие было доказано

> Необходимое условие: $f$ дифф. в т. $X_{0}\implies$
> $$
\Delta f(x_{0})=A_{1}\Delta x_{1}+\dots +A_{n}\Delta x_{n}+\bar{o}(|\Delta X|)
$$ Возьмём $\Delta X=(0,\dots,0,\Delta x_{i};0,\dots,0)$
>  Тогда $f(x_{10},\dots x_{i-1,0},x_{i+1,0},\dots,x_{n,0})-f(x_{10},\dots,x_{i_{0}},\dots,x_{n_{0}})=A_{i}\Delta x_{i}+\bar{o}(|\Delta x_{i}|)\implies \frac{\Delta \phi_{i}(x_{i,0})}{\Delta x_{i}}=A_{i}+\frac{\bar{o}(|\Delta x_{i}|)}{\Delta x_{i}}$, где $\phi_{i}(x_{i})=f(x_{10},\dots,x_{i-1,0},x_{i},x_{i+1,0},\dots,x_{n0})$; берём $\lim_{ \Delta x_{i} \to \infty } : \phi_{i}'(x_{i0})=A_{i} \iff \frac{\partial f}{\partial x_{i}}(x_{0})=A_{i}$


### Определение

> Дифференциалом функции f в точке $X_{0}$ называется главная линейная часть ее приращения в точке $X_{0}$ : $df(X_{0})=\sum_{i=1}^{n} \frac{\partial f}{\partial x_{i}}(x_{0})\cdot\Delta x_{i}$, предполагается, что f дифф. в точке $X_{0}$


### Замечание

> Рассмотрим функцию $F_{i}(X)=x_{i}$ $(X=(x_{1},\dots,x_{n}))$

#### Определение 

> $dF_{i}(X_{0})=0\cdot\Delta x_{1}+\dots,0\cdot\Delta x_{i-1}+1\cdot\Delta x_{i}+\dots+0\cdot \Delta x_{n}=\Delta x_{i}$, т.е. $dx_{i}=\Delta x_{i}\implies df(X_{0})=\sum_{i=1}^{n} \frac{\partial f}{\partial x_{i}}(X_{0})dx_{i}$

### Геометрический смысл дифференциала 

Пусть $z=f(x,y)$  - [[Функция]] 2-x переменных, определенная в $U(x_{0},y_{0})$ и дифф. в точке $(x_{0},y_{0})$. Рассмотрим поверхность $S=\{ (x,y,f(x,y)), (x,y) \in U(x_{0},y_{0}) \}$
это прямая, состоящая из след. точек:

![[2024-02-16 Lecture - Ангем 2024-02-16 14.54.45.excalidraw]]

![[2024-02-16 Lecture - Ангем 2024-02-16 14.56.38.excalidraw]]


### Определение 

> Плоскость P над касательной плоскостью к поверхности S в т. $(x_{0},y_{0})$, если:
> 1. т. $M_{0}(x_{0},y_{0},f(x_{0},y_{0}))\in P$
> 2. $\rho(M_{xy},M_{xy}')=\bar{o}(|\Delta X|)=\bar{o}(\sqrt{ (x-x_{0})^{2}+(y-y_{0})^{2} })$
> где $M_{xy}=(x,y,f(x,y))\in S$, $M_{xy}'=(x,y,z) \in P$
> 

### Теорема

> Th. Если $f(x,y)$ дифф. в точке $(x_{0},y_{0})$, то касательная плоскость к поверхности $S$ в т. $(x_{0},y_{0})$ существует и задается уравнением:
> $$
\begin{gather}
z-z_{0}= \frac{\partial f}{\partial x}(x_{0},y_{0})(x-x_{0})+\frac{\partial f}{\partial x}(x_{0},y_{0})(y-y_{0}), \\
\text{где } z_{0}=f(x_{0},y_{0}).
\end{gather}
$$

![[2024-02-16 Lecture - Ангем 2024-02-16 15.07.51.excalidraw]]

> Заметим, что уравнение в начале конуса не дифференициируемо

$$
\begin{gather}
\frac{x^{2}}{a^{2}}+ \frac{y^{2}}{b^{2}} - \frac{z^{2}}{c^{2}}=0, \\
z =c\sqrt{ \frac{x^{2}}{a^{2}}+\frac{y^{2}}{b^{2}} } - \text{ \ верхняя половина \ } \\
z'_{x} = \frac{c^{\frac{2x}{a^{2}}}}{2\sqrt{ \dots }} \\
z_{y}'= \frac{c^{\frac{y}{b^{2}}}}{2\sqrt{\dots  }}
\end{gather}
$$

### Доказательство

1) $M_{0}(x_{0},y_{0},\underbrace{ f(x_{0},y_{0}) }_{ =z_{0} })\in P$
2) $$\rho(M_{xy}, M'_{xy})=|f(x,y)-\underbrace{ z_{0} }_{ f(x_{0},y_{0}) }- \frac{\partial f}{\partial x}(X_{0})\underbrace{ (x-x_{0}) }_{ \Delta x }-\frac{\partial f}{\partial x}(X_{0})\underbrace{ (y-y_{0}) }_{ \Delta y }|=|\Delta f(X_{0})-df(X_{0})|=|\bar{o}(\Delta X)|$$
При $|\Delta X|\to_{0}$

### Замечание 

> Уравнение касательной плоскости: $z=z_{0}+df(x_{0},y_{0})$
> [[Дифференциал]] определяет уравнение касательной плоскости

### Поговорим о сложной функции от двух переменных (суперпозиция функций)

> Пусть $f(x_{1}, \dots, x_{n})$ определена в $U(x_{0})$, $X_{0}=(x_{10},\dots,x_{n0})$, пусть $\phi_{1}(t), \dots, \phi_{n}(t)\text{ \ определены в \ } U(t_{0})$, причем $(\phi_{1}(t_{0}),\dots,\phi_{n}(t_{0}))=X_{0}$ 
> $\{ (\phi_{1}(t_{0}),\dots,\phi_{n}(t)), t \in U(t_{0}) \}$

![[2024-02-16 Lecture - Ангем 2024-02-16 15.26.52.excalidraw]]

$F(t)=f(\phi_{1}(t),\dots,\phi_{n}(t))$ - сложная функция

### Теорема

> Пусть $f(X)$ дифф. в т. $X_{0}$, $\phi_{1}(t),\dots \phi_{n}(t)$ дифф. в т. $t_{0}$. Тогда $F(t)$ дифф. в т. $t_{0}$, причем $F'(t_{0})=\sum_{i=1}^{n} \frac{\partial f}{\partial x_{i}}(X_{0})\cdot \phi_{i}'(t_{0})$

#### Proof

> $f \text{ \ дифф. в т. \ }X_{0}\implies f(X_{0}+\Delta X)-f(X_{0})=\sum_{i=1}^{n} \frac{\partial f}{\partial x_{i}}(X_{0})\Delta x_{i}+\bar{o}(|\Delta X|)$

> Пусть $\Delta X=(\phi_{1}(t)-\phi_{1}(t_{0})),\dots,\phi_{n}(t)-\phi_{n}(t_{0})$
> Тогда $f(\phi_{1}(t), \dots, \phi_{n}(t))-f(\phi_{1}(t_{0}),\dots,\phi_{n}(t_{0}))=\sum_{i=1}^{n} \frac{\partial f}{\partial x_{i}}(X_{0})(\phi_{i}(t)-\phi_{i}(t_{0}))+\bar{o}(|\Delta X|)$
> $\implies \underbrace{ F(t)-F(t_{0}) }_{ \Delta F }=\sum\dots$
> Поделим обе части на $\Delta t=t-t_{0}$:
> $\frac{\Delta F}{\Delta t}=\sum_{i=1}^{n}(X_{0}) \frac{\Delta \phi_{i}}{\Delta t}+ \frac{\bar{o}(|\Delta X|)}{\Delta t}$
> берем $\lim_{ \Delta t \to 0 }$
> $\lim_{ \Delta t \to 0 } \frac{\Delta F}{\Delta t}=\sum_{i=1}^{n} \frac{\partial f}{\partial x_{i}}(X_{0})\cdot \phi_{i}'(t_{0})+\lim_{ \Delta t \to 0 } \frac{\bar{o}(|\Delta X|)}{\Delta t};$
> $\lim_{ \Delta t \to 0 } \frac{\bar{o}(|\Delta X|)}{\Delta t}=\lim_{ \Delta t \to 0 } \frac{\bar{o}(|\Delta X|)}{\Delta t}=\lim_{ \Delta t \to 0 } \frac{\bar{o}(\sqrt{ (\Delta \phi_{1})^{2}+\dots+\Delta (\phi_{n})^{2} }}{\Delta t}=$
> $=\lim_{ \Delta t \to 0 } \frac{\bar{o}(\sqrt{ (\Delta \phi_{1})^{2}+\dots+\Delta (\phi_{n})^{2} }}{(\sqrt{ \Delta \phi_{1})^{2}+\dots+\Delta (\phi_{n})^{2}  })}\cdot \frac{\sqrt{ \Delta (\phi_{1})^{2}+\dots+\Delta (\phi_{n})^{2}  }}{\Delta t}=$
> $=\lim_{ \Delta t \to 0 } \frac{\bar{o}(\sqrt{ \dots })}{\sqrt{ \dots }}\cdot \sqrt{ \left( \frac{\Delta \phi_{1}}{\Delta t} \right)^{2}+\dots+ \left( \frac{\Delta \phi_{n}}{\Delta t} \right)^{2}  }$ (ля $\Delta t>0$)
> $=\text{ \ то же выражение от \ }t_{0} = 0$


Таким образом, $\exists \lim_{ \Delta t \to 0 } \frac{\Delta F}{\Delta t}=\sum_{i=1}^{n} \frac{\partial f}{\partial x_{i}}(X_{0})\cdot \phi_{i}'(t_{0})=F'(t_{0})$
