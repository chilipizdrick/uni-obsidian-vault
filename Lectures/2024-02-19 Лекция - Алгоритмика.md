---
tags:
  - lecture
  - 2nd-semester
  - 2024-02-19
---
```julia
typeof(Int64) # DataType
typeof(Complex) # UnionAll
```
## Функция для вывода дерева типов
```julia
funciton subtype_tree!(type::Union{DataType, UnionAll}, indentation::String)
	print(indentation);
	println(type);
	for subtype in subtypes(type)
		indentation += indentaition
		subtype_tree(subtype, indentation)
	end
end
```